# Install as /etc/boomer-erp/workers.env on the Tencent fixed-egress host only.
# run-tencent-erp.sh ignores this file and disables both workers on candidate ports.
HANDHELD_RELEASE_WORKER_ENABLED=true
HANDHELD_ITEM_SYNC_WORKER_ENABLED=true
