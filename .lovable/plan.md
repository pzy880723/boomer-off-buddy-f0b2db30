我查到数据库里门店没有丢：现在还有 2 家 active 门店：总部 `BOOMER OFF vintage` 和分店 `BOOMER OFF vintage（中信泰富店）`。页面显示 0 家更像是前端/Server Function 读取失败或缓存没有刷新，而不是数据被删了。

计划如下：

1. 修复门店列表读取
   - 让 `/youzan` 的门店列表在读取失败时显示明确错误，而不是直接落到“还没有连接任何门店”。
   - 调整 `listYouzanShops` 的查询排序，避免因为字段/类型/排序导致接口异常时页面误判为空。
   - 同步完成、添加门店、移除门店后同时刷新门店列表、统计和同步日志缓存。

2. 防止“空状态误导”
   - 区分三种状态：加载中、读取失败、真的没有门店。
   - 如果后端已有门店但同步统计暂未更新，页面仍优先展示门店卡片。

3. 继续修商品和订单同步
   - 保留当前 active 门店作为同步对象，不依赖页面上的列表长度判断实际数据库是否有店。
   - 检查同步日志展示，确保有赞 API 返回 0、解析失败、保存失败都能在高级同步明细里看到原因。

4. 验证
   - 读取数据库确认 2 家门店仍存在。
   - 打开 `/youzan` 确认页面显示总部和分店，不再显示“还没有连接任何门店”。
   - 再触发一键同步，检查商品、订单、同步日志数量是否变化。